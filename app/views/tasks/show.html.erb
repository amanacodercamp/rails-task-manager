<h1>Select a Task</h1>
<p><%= @task.title %></p>
<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<%= link_to "Edit", edit_task_path(@task) %>
